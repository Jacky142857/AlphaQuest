# Minimal requirements that definitely work with Python 3.13
Django==4.2.16
djangorestframework==3.14.0
django-cors-headers==4.3.1
gunicorn==21.2.0
whitenoise==6.6.0

# Basic math libraries (install separately to avoid conflicts)
numpy
pandas
pymongo
dnspython

# Add these back one by one if needed:
# sympy
# scipy
# yfinance